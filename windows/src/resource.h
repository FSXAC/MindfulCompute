#pragma once
// Resource IDs shared by the C++ sources and resources.rc (kept in sync by hand
// -- if you add one here, add it there too).
#define IDI_APPICON 101   // app / window / tray-idle icon (multi-size .ico)
#define IDR_BOWL    201   // Tibetan bowl, embedded WAVE resource (PlaySound SND_RESOURCE)
